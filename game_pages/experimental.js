const canvas = document.getElementById('canvas');
const ctx = canvas.getContext('2d');

const CNAVAS_WIDTH = canvas.tWidth = 600;
const CANVAS_HEIGHT = canvas.height = 600;

const platerImage = new Image();
console.log(ctx);